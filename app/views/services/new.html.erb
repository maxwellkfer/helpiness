<div class="container">
  <h2>Post an offer :</h2>
  <%= simple_form_for @service do |f| %>
    <%= f.input :name, maxlength: 15 %>
    <%= f.input :rating %>
    <%= f.input :helpies %>
    <%= f.association :category %>
    <%= f.submit "add a service", class: "btn btn-primary" %>
  <% end %>
</div>

<!-- add a picture button for users to add photos of service -->
